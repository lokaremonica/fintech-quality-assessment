# Examples only: export these in your shell. No implicit .env loading.
TEST_ENV=local
MOCK_PORT=3100
# Optional overrides for an already-running compatible demo:
# START_MOCK=false
# API_BASE_URL=http://127.0.0.1:3100
# UI_BASE_URL=http://127.0.0.1:3100
# MOCK_AUTH_SECRET=shared-demo-only-secret-at-least-32-characters
